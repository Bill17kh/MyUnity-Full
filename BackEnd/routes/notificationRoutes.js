// notificationRoutes.js content placeholder

const express = require('express');
const router = express.Router();

router.get('/', (req, res) => {
  res.send('List notifications - placeholder');
});

router.post('/', (req, res) => {
  res.send('Send notification - placeholder');
});

module.exports = router;
